// Background service worker

// Enable side panel on action click if we want action to toggle side panel
// chrome.sidePanel.setPanelBehavior({ openPanelOnActionClick: true }).catch(console.error);

console.log("BookmarkHero background worker started");
